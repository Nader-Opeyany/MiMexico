@model IEnumerable<Meat>
@{
    ViewData["Title"] = "Index";
}
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Meat List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Meat" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Meat
            </a>
        </div>
    </div>
    <br />

    <table class="table table-bordered table-danger-striped" style="width:100%">
        <thread>
            <tr>
                <th>
                    Meat Name
                </th>
                <th></th>
            </tr>
        </thread>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="50%">
                        @obj.name
                    </td>

                    
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Meat" asp-action="Delete" asp-route-id="@obj.id" class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


